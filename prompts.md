Write a script that accepts the ship and the contract and runs a mining loop that goes to an asteroid field, mines the required thing, and delivers it as in the contract. Do not run the script yet.

Abstract the api function in that file to a separate file that can manage api calls from multiple active scripts without exceeding the rate limit.