# Required: your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: pre-seed a SpaceTraders token instead of registering via the UI.
# If omitted, register through the web UI and the token is saved automatically.
# SPACETRADERS_TOKEN=
